<h1>Burgers!</h1>

<div>
<h2>Devoured Burgers!</h2>
<ul>
  {{#each cats}}
    {{#unless sleepy}}
      {{> cats/cat-block sleep=true}}
    {{/unless}}
  {{/each}}
</ul>
</div>

<div>
<h2>Soon-to-be-eaten Burgers!</h2>
<ul>
  {{#each cats}}
    {{#if sleepy}}
      {{> cats/cat-block sleep=false}}
    {{/if}}
  {{/each}}
</ul>
</div>

<div>
<h2>Add a New Burger!</h2>
<form class="create-form">
  <div class="form-group">
    <label for="burger">Burger Name:</label>
    <input type="text" id="burger" name="name">
  </div>

  <div class="form-group">
    <label for="slee">Devoured Yet?</label><br>
    <input type="radio" name="sleepy" value="1" checked> Of course!<br>
    <input type="radio" name="sleepy" value="0"> Nope, not yet!
  </div>

  <button type="submit">Add Burger</button>
</form>
</div>
